import { MoveValidationResult } from './move-validation-result';

describe('MoveValidationResult', () => {
  it('should create an instance', () => {
    expect(new MoveValidationResult()).toBeTruthy();
  });
});
